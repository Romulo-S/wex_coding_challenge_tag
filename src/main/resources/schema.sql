CREATE TABLE PURCHASE_TRANSACTION (
                                      ID BIGINT AUTO_INCREMENT PRIMARY KEY,
                                      AMOUNT DECIMAL(10, 2),
                                      TRANSACTION_DATE DATE
);
